import { ContrapipePipe } from './contrapipe.pipe';

describe('ContrapipePipe', () => {
  it('create an instance', () => {
    const pipe = new ContrapipePipe();
    expect(pipe).toBeTruthy();
  });
});
